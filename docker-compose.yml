services:
  dentalflow-web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    labels:
      - traefik.enable=true
      - traefik.http.services.dentalflow.loadbalancer.server.port=80
